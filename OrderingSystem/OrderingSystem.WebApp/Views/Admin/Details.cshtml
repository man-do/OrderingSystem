@model OrderingSystem.Data.Data.Models.UserViewModel

@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
<div>
        <h2>Details</h2>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.UserName)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.UserName)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Email)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Email)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Role)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Role.Name)
            </dd>
        </dl>
</div>
<p>
    @Html.ActionLink("Back", "Index", "Admin", null, new { @class = "btn btn-primary" })
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }, new { @class = "btn btn-warning" })
    @Html.ActionLink("Reset Password", "ResetPassword", new { id = Model.Id }, new { @class = "btn btn-warning" })
</p>
@*<script>
    $(document).ready(function () {
        $('#edit-btn').click(function () {
            var id = $('#value').val();
            $.ajax({
                url: "/Admin/Edit?Id=" + id,
                method: "GET",
                contentType: "application/json",
                success: function (data) {
                    $('#user-template').html(data);
                },
                error: function () {
                    console.log("Deshtoi !");
                }
            });
        });
    });
</script>*@